<h1><%= @city.name %></h1>

<% if @gossips.any? %>
  <p><strong>Potins :</strong></p>
  <ul>
    <% @gossips.each do |gossip| %>
      <li>
        <%= gossip.description %>
        <span class="text-muted">(écrit par <%= gossip.user.first_name %> <%= gossip.user.last_name %>)</span>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>Aucun gossip disponible pour cette ville.</p>
<% end %>
